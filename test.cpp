#include <iostream>
#include <fstream>
using namespace std;
    /* ?????????????????
    #include <iostream.h>
    #include <fstream.h>
    ???????????*/

int main(){
    ofstream fout;
    double a[100];  //??????????
    fout.open("file.txt", ios::out|ios::binary|ios::trunc);
    //  ???????
    //  ios::out ????????????
    //  ios::binary ??????????????????????????
    //  ios::trunc???????????????
    //  ios::add?????????

    if (!fout) {
        cout << "???? file.txt ??????";
        return 1;
    }
        //  ???????????????????

    for(int i=0;i<100;i++){
        a[i]=1.0*double(i);
        fout.write(( char * ) &a[i],sizeof( double ) );
        //???????????????
        // ?sizeof( double )??????char *??????a[i]??????????
    }

    fout.close();  //????????

	char outfile[] = "file.txt";  //???????????
     
    ifstream fin( outfile, ios::in | ios::binary );
    //  ???????
    //  ios::in ???????  ios::binary ???????

    
    if (!fin){
        cout << "???? file.txt ??????";
        return 1;
    }
    //  ????????????????
    
    double d;  //??????????
    
    //while(!fin.eof()){  //????????????
    //while(){  //????????????
    //
    int k = 5;
    fin.seekg ( k*sizeof ( double ) );
    for(int i=0; i<5; i++){
        fin.read( ( char * ) &d, sizeof( double ) );
          //???????????????
        cout << d << endl; 
    }
    
    fin.close();  //????????
    
    return 0;
}
